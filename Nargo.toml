[package]
name = "noir_gpr_ed25519"
type = "bin"
authors = ["vkpatva"]
compiler_version = ">=0.36.0"

[dependencies]
bignum = { path = "vendor/bignum" }
sha256 = { tag = "v0.3.0", git = "https://github.com/noir-lang/sha256" }
